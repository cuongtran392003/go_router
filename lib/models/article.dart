class Article {
  int id;
  String image;
  String title;
  String description;

  // 🔥 Thêm constructor để khởi tạo đối tượng
  Article({
    required this.id,
    required this.image,
    required this.title,
    required this.description,
  });
}